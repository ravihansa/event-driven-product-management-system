model Category {
  id          Int @id @default(autoincrement())
  name        String
  categoryKey String
  products    Product[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
